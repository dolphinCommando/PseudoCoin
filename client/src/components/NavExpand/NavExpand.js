import React from 'react';
import './NavExpand.css';

const NavExpand = props => (
  <div></div>
);

/*
if you need to do logic on props, try
const NavExpand = props => {
  const newProps = <do stuff to props>
  return (
    <div>{newProps.exampleProperty}</div>
  )
}
*/

export default NavExpand;